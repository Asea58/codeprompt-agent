# codeprompt Agent 使用教程

本教程教你**从零上手**这个工具：怎么构造一道题、在哪里输入、输入什么、以及如何扩展学科 / 考点 / 输出格式。

> 一句话理解它：**你给「学科 + 不能手算的原因」，它自动造一道物理编程题，并通过真实运行代码得到可验证的标准答案。**

> 两种用法：**网页版**（推荐，点点鼠标即可）和**命令行版**（适合批量/脚本）。两者共用同一套核心引擎，结果完全一致。

---

## 目录

1. [准备工作（只做一次）](#1-准备工作只做一次)
2. [网页版用法（推荐）](#2-网页版用法推荐)
3. [命令行版用法](#3-命令行版用法)
4. [应该输入什么：参数详解](#4-应该输入什么参数详解)
5. [生成结果在哪里看](#5-生成结果在哪里看)
6. [在哪里修改「学科方向」](#6-在哪里修改学科方向)
7. [在哪里修改「不能手算的原因」](#7-在哪里修改不能手算的原因)
8. [在哪里调整「输出格式」](#8-在哪里调整输出格式)
9. [其他可调项（模型 / 提示词）](#9-其他可调项模型--提示词)
10. [常见问题排查](#10-常见问题排查)
11. [工作原理（一图看懂）](#11-工作原理一图看懂)

---

## 1. 准备工作（只做一次）

### 1.1 安装依赖

```bash
cd "/Users/v_wudongpei/Downloads/codeprompt Agent"
python3 -m pip install -r requirements.txt
```

> 💡 这台机器上没有 `pip` 命令，要用 `python3 -m pip`。若提示权限问题，加 `--user`：
> `python3 -m pip install --user -r requirements.txt`

依赖说明：`numpy`、`scipy`（生成的题目代码要用）、`openai`（调 DeepSeek）、`gradio`（网页前端）、`huggingface_hub<1.0`（gradio 4.x 的兼容约束，**必须，否则网页起不来**）。

### 1.2 设置 DeepSeek API Key

Key 通过**环境变量**传入，绝不写进任何文件：

```bash
export DEEPSEEK_API_KEY=sk-你的key
```

> 这条 `export` 只在当前终端窗口有效。每次新开终端都要重设，或写进你的 `~/.zshrc`。

### 1.3 先跑一次 mock，确认环境没问题（不消耗 API）

```bash
python3 cli.py --subject 力学 --reason 事件检测求阈值时刻 --mock
```

看到 `✅ 出题成功` 就说明管线通了，可以正式用了。

---

## 2. 网页版用法（推荐）

最直观的用法：开一个网页，下拉选学科和考点，点按钮出题。

### 2.1 启动网页

```bash
cd "/Users/v_wudongpei/Downloads/codeprompt Agent"
export DEEPSEEK_API_KEY=sk-你的key
python3 app.py
```

启动成功后终端会打印：

```
Running on local URL:  http://127.0.0.1:7860
```

**用浏览器打开这个地址 `http://127.0.0.1:7860`** 即可。

> 终端里那行 `NotOpenSSLWarning` 是无害警告，忽略即可。

### 2.2 怎么用界面

界面分左右两栏：

```
┌─────────────────────────┬──────────────────────────────┐
│  学科方向   [力学    ▼]  │  ✅ 出题成功  答案：-1.8       │
│  考点      [混沌… ▼]    │  ┌答案─────┐ ┌单位/含义────┐  │
│  （考点说明自动显示）     │  │ -1.8    │ │ y坐标，单位m │  │
│                         │  ├─ 题面 Query ─────────────┤  │
│  ▸ 高级选项             │  │ (渲染好的题面、公式)        │  │
│    审核模式 (•)warn …    │  ├─ 思路/代码/校验 ──────────┤  │
│    重试次数 [==2==]      │  │ (思路 + 代码 + 运行结果)    │  │
│    超时    [==60==]     │  └────────────────────────────┘  │
│    □ mock 模式          │  [ 📥 下载完整 .md ]            │
│    [ 🚀 生成题目 ]      │                                │
└─────────────────────────┴──────────────────────────────┘
```

操作步骤：

1. **选「学科方向」**（下拉框，来自 `config/subjects/`）。
2. **选「不能手算的原因」**（下拉框，来自 `config/reasons.json`）。选中后，下方会**自动显示该考点的数学结构、数值方法、约束**，帮你理解会出什么样的题。
3. （可选）展开 **「高级选项」** 调整审核模式 / 重试次数 / 超时；想先不耗 API，勾上 **「mock 模式」** 用内置样例体验界面。
4. 点 **🚀 生成题目**，稍候片刻。
5. 右栏查看结果：**答案**单独高亮，**题面**和**思路/代码/校验**分两个标签页，还能点 **📥 下载完整 .md**。

> ✨ 关键特性：下拉框的选项**直接来自 `config/`**。你以后新增了学科或考点（见 §6、§7），**刷新网页就会自动出现**，不用改 `app.py`。

### 2.3 想分享给别人用

加 `--share` 会生成一个临时公网链接（72 小时有效），同事在自己浏览器就能打开：

```bash
python3 app.py --share
```

也可换端口或对局域网开放：

```bash
python3 app.py --port 8000          # 换端口
python3 app.py --host 0.0.0.0       # 同一局域网内其他设备可访问
```

> ⚠️ 安全提醒：开放给别人意味着「别人点按钮、在你的机器上运行 LLM 生成的代码」。**内部小范围用没问题**；若要长期对外开放，需要给代码执行加更强的沙箱（见 §10）。另外 API Key 只留在你后端的环境变量里，不会泄露给浏览器。

### 2.4 关掉网页

在终端按 `Ctrl + C` 即可停止。

---

## 3. 命令行版用法

适合批量生成、写脚本、或不想开浏览器时。

### 3.1 在哪里输入？

**在终端里运行 `cli.py`**。基本格式：

```bash
python3 cli.py --subject <学科> --reason <不能手算的原因>
```

### 3.2 三个真实例子

```bash
# 力学 + 混沌系统
python3 cli.py --subject 力学 --reason 混沌系统对初值敏感

# 热学 + 边值问题打靶法
python3 cli.py --subject 热学 --reason 非线性边值问题打靶法

# 电动力学 + 有限差分
python3 cli.py --subject 电动力学 --reason 非线性边值问题有限差分
```

### 3.3 忘了有哪些可选值？用 `--list`

```bash
python3 cli.py --list
```

它会列出**所有可用的学科**和**所有可用的考点**，照着抄即可：

```
可用学科 (--subject):
  - 力学
  - 热学
  - 电动力学

可用考点 / 不能手算的原因 (--reason):
  - 非线性ODE初值问题
  - 非线性边值问题打靶法
  - 非线性边值问题有限差分
  - 事件检测求阈值时刻
  - 混沌系统对初值敏感
  - 变系数微分方程
```

> ⚠️ `--subject` 和 `--reason` 的取值**必须和 `--list` 里完全一致**（含中文）。拼错会报错并提示可选值。
> 网页版无需记这些——下拉框直接列好了。

---

## 4. 应该输入什么：参数详解

下面参数**命令行和网页通用**（网页里对应「高级选项」）。

| 参数 | 必填 | 说明 | 示例 |
|---|---|---|---|
| `--subject` | ✅ | 学科方向，取值见 `--list` | `力学` |
| `--reason` | ✅ | 不能手算的原因（考点），取值见 `--list` | `混沌系统对初值敏感` |
| `--mock` | ❌ | 不调 API，用内置样例跑通管线（自测用） | — |
| `--judge` | ❌ | LLM 语义审核模式，见下 | `warn` |
| `--retries` | ❌ | 生成失败时的最大重试次数（默认 2） | `3` |
| `--timeout` | ❌ | 单次代码执行超时秒数（默认 60） | `90` |
| `--no-save` | ❌ | 不写出 markdown 文件（只看终端） | — |
| `--list` | ❌ | 列出可用学科与考点后退出 | — |

### `--judge` 三种模式怎么选

LLM 审核是「第二层把关」，判断题目是否真正契合考点、是否真的不能手算：

| 模式 | 行为 | 适用场景 |
|---|---|---|
| `warn`（默认） | 代码跑出正常数值即算通过；审核意见只写进报告做提示，不否决 | **日常出题，推荐** |
| `block` | 审核可以否决题目并触发重试 | 想要更严格的质量把关 |
| `off` | 完全关闭审核 | 最快、最省 token |

例如想严格把关：

```bash
python3 cli.py --subject 力学 --reason 非线性ODE初值问题 --judge block
```

---

## 5. 生成结果在哪里看

无论网页版还是命令行版，结果都有两个去处：**界面/终端**和**落盘文件**。

### 5.1 界面 / 终端

- **网页版**：右栏直接显示答案、题面、代码、校验报告（见 §2.2）。
- **命令行版**：终端实时显示进度，最后给出答案：

```
=== 第 1 次生成 (subject=电动力学, reason=非线性边值问题有限差分, mock=False) ===
  ✓ 已生成题面/代码
  ✓ 代码执行成功，ANSWER = 0.6812
  ✓ 自检通过（第 1 次）
✅ 出题成功（共 1 次生成）
   答案：0.6812
   已保存：.../output/电动力学__非线性边值问题有限差分__20260617_145731.md
```

### 5.2 落盘文件（两种方式都会保存）

每道题自动保存到 **`output/` 文件夹**，文件名格式为：

```
output/<学科>__<考点>__<时间戳>.md
```

网页版还可以点 **📥 下载完整 .md** 直接下载这份文件。打开它，里面是完整的「四件套」：

1. **Query** —— 完整题面（场景 + 参数 + 方程 + 求解任务 + 精度要求）
2. **Response** —— 解题思路 + 完整可运行代码 + 真实运行结果
3. **答案** —— 经代码执行验证的唯一数值
4. **校验报告** —— 自检结果

> `output/` 已被 `.gitignore` 忽略，不会污染 git 仓库。

---

## 6. 在哪里修改「学科方向」

### 📂 位置：`config/subjects/` 文件夹

每个学科是一个独立的 JSON 文件，**文件名就是学科名**：

```
config/subjects/
├── 力学.json
├── 热学.json
└── 电动力学.json
```

### 6.1 修改现有学科

直接编辑对应的 `.json`，比如想给「力学」增加新的出题情境，编辑 `config/subjects/力学.json` 的 `typical_scenarios` 列表。

### 6.2 新增一个学科（例如「光学」）

**复制一个现有文件改内容即可，无需改任何代码：**

1. 复制 `config/subjects/力学.json` → `config/subjects/光学.json`
2. 编辑内容（字段含义见下表）
3. 立即可用：命令行 `python3 cli.py --subject 光学 --reason ...`，或**刷新网页**后下拉框自动出现「光学」

字段说明：

| 字段 | 作用 |
|---|---|
| `subject` | 学科名（**要和文件名一致**） |
| `domain_description` | 学科概述，告诉模型这个领域是干什么的 |
| `typical_scenarios` | 典型物理情境清单，模型从中取材或借鉴 |
| `common_symbols` | 常用符号约定 |
| `typical_units` | 常用单位 |
| `style_notes` | 出题风格要求 |

> 💡 `typical_scenarios` 写得越具体、越贴合某类数值方法，生成的题目质量越高。

---

## 7. 在哪里修改「不能手算的原因」

### 📂 位置：`config/reasons.json` 文件

这是整个工具**最核心的配置**——它把抽象的「不能手算」锚定为具体的**数学结构 + 数值方法**，防止题目跑偏。

### 7.1 文件结构

每一个 key 就是一类考点，也是 `--reason` 的取值：

```jsonc
{
  "混沌系统对初值敏感": {
    "label": "混沌 / 高维强非线性耦合系统",
    "math_structure": "高维耦合非线性 ODE（双摆、耦合振子）...",
    "numerical_method": "高精度 solve_ivp（小容差、固定求解器）数值积分",
    "must_have": [
      "系统高维且强非线性耦合",
      "为保证可复现，固定求解器与容差",
      "..."
    ],
    "answer_pattern": "某时间窗内的极值 / 统计量",
    "pitfalls": "末态对步长/求解器极敏感..."
  }
}
```

### 7.2 各字段作用

| 字段 | 作用 | 重要性 |
|---|---|---|
| `label` | 人类可读的考点名 | — |
| `math_structure` | 该考点对应的**数学结构**（必须落到方程上） | ⭐⭐⭐ |
| `numerical_method` | 对应的数值方法 | ⭐⭐ |
| `must_have` | 生成题目**必须满足**的硬约束（自检会用到） | ⭐⭐⭐ |
| `answer_pattern` | 答案应该是什么类型的单一数值 | ⭐ |
| `pitfalls` | 常见坑，提醒模型规避 | ⭐⭐ |

### 7.3 新增一类考点

在 `config/reasons.json` 里**追加一条**即可（注意 JSON 逗号语法），主代码不用动：

```jsonc
"积分方程数值求解": {
  "label": "第二类 Fredholm 积分方程，需数值求积",
  "math_structure": "未知函数出现在积分号内，核函数非平凡",
  "numerical_method": "把积分离散为求和，转成线性方程组求解",
  "must_have": [
    "未知量在积分号内",
    "无法化简为初等积分",
    "结果取某点的函数值"
  ],
  "answer_pattern": "某点处的函数值",
  "pitfalls": "离散节点数不足会影响精度"
}
```

加完后命令行 `python3 cli.py --list` 就能看到它；**网页刷新**后下拉框也会自动出现。

> 💡 `math_structure` 和 `must_have` 写得越具体，模型越不容易跑偏，自检也越有效。

---

## 8. 在哪里调整「输出格式」

### 📂 位置：`templates/output_format.md` 文件

最终落盘的 `.md` 长什么样，完全由这个模板决定。它用 `{占位符}` 标记要填入的内容。

### 8.1 可用的占位符

| 占位符 | 含义 |
|---|---|
| `{subject}` | 学科名 |
| `{reason_label}` | 考点名 |
| `{check_status}` | 自检状态（通过/未通过） |
| `{query}` | 题面 |
| `{approach}` | 解题思路 |
| `{code}` | 完整代码 |
| `{run_output}` | 代码真实运行输出 |
| `{answer}` | 最终答案 |
| `{answer_unit}` | 答案单位与含义 |
| `{check_report}` | 校验报告 |

### 8.2 举例：想去掉「校验报告」一节

编辑 `templates/output_format.md`，删掉文件末尾这两行相关内容即可：

```markdown
## 校验报告

{check_report}
```

### 8.3 举例：想改成只输出题面和答案

把模板内容精简成：

```markdown
# {subject} · {reason_label}

## 题目
{query}

## 答案
{answer}（{answer_unit}）
```

> ⚠️ 只要不删除主代码会用到的占位符之外的内容，怎么排版都行。如果**删掉了某个占位符**也没关系，对应内容就不会出现在结果里。但**不要新增主代码不认识的占位符**，否则会报错。

---

## 9. 其他可调项（模型 / 提示词）

### 9.1 换模型 / 改模型参数 → `config/llm.json`

```jsonc
{
  "provider": "deepseek",          // 改这里切换模型
  "providers": {
    "deepseek": {
      "base_url": "https://api.deepseek.com",
      "model": "deepseek-chat",     // 模型名
      "api_key_env": "DEEPSEEK_API_KEY",
      "temperature": 0.7,           // 调高更发散，调低更稳定
      "max_tokens": 8000
    }
  }
}
```

- **想用更强的推理模型**：把 `provider` 改成 `"deepseek-reasoner"`（已预置）。
- **想接别的模型**：在 `providers` 里加一条（任何 OpenAI 兼容接口都行），再把 `provider` 指过去。

### 9.2 改出题的「口吻 / 总规则」 → `templates/system_prompt.md`

这是给模型的总指令（必须锚定考点、答案唯一可验证、代码必须打印 `ANSWER:` 等）。一般不用动，想全局调整出题风格时改这里。

### 9.3 改单次生成的提示结构 → `templates/generation_prompt.md`

控制每次把学科/考点信息怎么组织后发给模型，以及要求模型用四个 XML 标签输出。改它会影响解析，**改前请理解 `src/generator.py` 的解析逻辑**。

---

## 10. 常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| `command not found: pip` | 这台机器没有 `pip` 命令 | 改用 `python3 -m pip ...` |
| `环境变量 DEEPSEEK_API_KEY 未设置` | 没 export key | 见 [1.2](#12-设置-deepseek-api-key)，或先用 `--mock` / 勾选网页 mock |
| `未知的 subject 'xxx'` | 学科名拼错 | 用 `--list` 核对，或去 `config/subjects/` 加文件 |
| `未知的 reason 'xxx'` | 考点名拼错 | 用 `--list` 核对，或去 `config/reasons.json` 加条目 |
| `代码运行报错 / 执行超时` | 模型生成的代码有问题或太慢 | 工具会自动重试；可调大 `--timeout`、`--retries` |
| 题目质量不满意 / 跑偏 | 考点约束太松 | 在 `reasons.json` 把 `math_structure`、`must_have` 写更具体；或用 `--judge block` |
| 反复触发审核否决 | 审核过严 | 用默认 `--judge warn`，或 `--judge off` |
| 网页 `cannot import name 'HfFolder'` | huggingface_hub 版本太新 | `python3 -m pip install "huggingface_hub<1.0"` |
| 网页 `argument of type 'bool' is not iterable` / `localhost is not accessible` | gradio 4.x 与新依赖的已知兼容 bug | `app.py` 已内置兼容补丁，直接重跑即可；仍不行则 `python3 -m pip install "gradio==4.31.5" "gradio_client==1.2.1" "huggingface_hub<1.0"` |
| 浏览器打不开 `127.0.0.1:7860` | 端口被占用 | `python3 app.py --port 8000` 换端口 |
| `NotOpenSSLWarning` | 无害警告 | 忽略即可 |

---

## 11. 工作原理（一图看懂）

```
你输入：学科 + 不能手算的原因         （网页下拉框 或 命令行 --subject/--reason）
        │
        ▼
[1] 读配置   config/subjects/<学科>.json + config/reasons.json
        │
        ▼
[2] 组装提示  templates/system_prompt.md + generation_prompt.md
        │
        ▼
[3] 调模型    DeepSeek 生成「题面 + 思路 + 代码」(四个 XML 标签)
        │
        ▼
[4] 真实执行  ★在子进程跑代码，抓 ANSWER: 行★  ← 答案绝不靠模型臆造
        │
        ▼
[5] 自检      代码是否跑通？非线性是否到位？考点是否吻合？
        │           ├─ 不通过 → 带反馈重试（回到 [3]）
        ▼
[6] 落盘      按 templates/output_format.md 渲染 → output/*.md
```

> 网页版（`app.py`）和命令行版（`cli.py`）只是 [1] 之前的「输入入口」和 [6] 之后的「展示方式」不同，**中间 [1]~[6] 是同一套引擎 `src/pipeline.py`**，结果完全一致。

**核心思想**：会变的东西（学科、考点、格式、模型）全部外置成 `config/` 和 `templates/` 里的文件，主代码 `src/` 只负责跑流程。所以**日常扩展基本只改配置，不碰代码**——这也方便多人协同。

> 🔒 关于安全：第 [4] 步会真实运行模型生成的代码。本机自己用风险可控；若用 `app.py --share` 对外开放，等于让外部用户在你的机器上触发代码执行，正式部署前应给 `src/executor.py` 加更强的沙箱隔离。

---

## 速查表

| 我想做什么 | 去哪里 |
|---|---|
| **开网页出题（推荐）** | 终端 `python3 app.py` → 浏览器开 `127.0.0.1:7860` |
| 分享网页给别人 | `python3 app.py --share` |
| 命令行出一道题 | `python3 cli.py --subject X --reason Y` |
| 看有哪些学科/考点 | `python3 cli.py --list`（网页下拉框已列好） |
| 看生成结果 | `output/` 文件夹（网页可直接下载） |
| 加 / 改学科 | `config/subjects/*.json` |
| 加 / 改考点（不能手算的原因） | `config/reasons.json` |
| 改输出格式 | `templates/output_format.md` |
| 换模型 / 调参 | `config/llm.json` |
| 改出题总规则 | `templates/system_prompt.md` |
