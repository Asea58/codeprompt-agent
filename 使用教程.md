# codeprompt Agent 使用教程

输入「**学科 + 不能手算的原因**」，自动构造一道物理编程题，并通过**真实运行代码**得到可验证的标准答案。

两种用法：**网页版**（推荐）和**命令行版**（适合批量/脚本），共用同一套引擎，结果一致。

---

## 目录

1. [准备工作（只做一次）](#1-准备工作只做一次)
2. [网页版用法（推荐）](#2-网页版用法推荐)
3. [命令行版用法](#3-命令行版用法)
4. [参数说明](#4-参数说明)
5. [结果在哪里看](#5-结果在哪里看)
6. [如何扩展（学科 / 考点 / 格式 / 模型）](#6-如何扩展学科--考点--格式--模型)
7. [Git 协同与版本更新](#7-git-协同与版本更新)
8. [常见问题排查](#8-常见问题排查)
9. [工作原理](#9-工作原理)

---

## 1. 准备工作（只做一次）

```bash
cd "/Users/v_wudongpei/Downloads/codeprompt Agent"

# 安装依赖（本机没有 pip 命令，用 python3 -m pip；权限报错就加 --user）
python3 -m pip install -r requirements.txt

# 设置 DeepSeek API Key（只在当前终端有效，可写进 ~/.zshrc 永久生效）
export DEEPSEEK_API_KEY=sk-你的key

# 不耗 API 跑一次，确认环境 OK，看到「✅ 出题成功」即可
python3 cli.py --subject 力学 --reason 事件检测求阈值时刻 --mock
```

---

## 2. 网页版用法（推荐）

```bash
export DEEPSEEK_API_KEY=sk-你的key
python3 app.py
```

启动后终端打印 `Running on local URL: http://127.0.0.1:7860`，浏览器打开该地址即可。

**操作：** 左栏选「学科」+「不能手算的原因」（选中会自动显示该考点的数学结构说明）→ 可展开「高级选项」调参或勾「mock 模式」→ 点 **🚀 生成题目** → 右栏看答案、题面、代码，可下载 .md。`Ctrl+C` 关闭。

**分享给别人：**

```bash
python3 app.py --share      # 生成 72 小时临时公网链接
python3 app.py --port 8000  # 换端口
```

> ✨ 下拉框选项直接来自 `config/`，新增学科/考点后刷新网页即出现，无需改 `app.py`。
> ⚠️ 对外开放 = 别人在你机器上跑模型生成的代码，仅建议内部小范围用（安全见 §9）。

---

## 3. 命令行版用法

```bash
# 基本格式
python3 cli.py --subject <学科> --reason <不能手算的原因>

# 例
python3 cli.py --subject 力学 --reason 混沌系统对初值敏感
python3 cli.py --subject 热学 --reason 非线性边值问题打靶法

# 忘了有哪些可选值？
python3 cli.py --list
```

> ⚠️ `--subject` / `--reason` 取值须和 `--list` 完全一致（含中文）。网页版无需记，下拉框已列好。

---

## 4. 参数说明

| 参数 | 必填 | 说明 |
|---|---|---|
| `--subject` | ✅ | 学科方向，见 `--list` |
| `--reason` | ✅ | 不能手算的原因（考点），见 `--list` |
| `--mock` | ❌ | 不调 API，用内置样例跑通管线 |
| `--judge` | ❌ | 审核模式：`warn`(默认,仅提示) / `block`(可否决重试) / `off`(关闭) |
| `--retries` | ❌ | 失败最大重试次数（默认 2） |
| `--timeout` | ❌ | 单次代码执行超时秒数（默认 60） |
| `--no-save` | ❌ | 不写出 .md 文件 |
| `--list` | ❌ | 列出可用学科与考点 |

---

## 5. 结果在哪里看

- **界面/终端**：网页右栏 / 命令行终端实时显示答案。
- **落盘文件**：每道题自动存到 `output/<学科>__<考点>__<时间戳>.md`，含「四件套」——题面 Query、解题思路+代码+运行结果、验证答案、校验报告。网页可直接「📥 下载完整 .md」。

> `output/` 已被 `.gitignore` 忽略，不进版本库。

---

## 6. 如何扩展（学科 / 考点 / 格式 / 模型）

**核心理念：会变的东西都在 `config/` 和 `templates/`，扩展基本不碰 `src/` 主代码。**

| 想做什么 | 改哪里 | 怎么改 |
|---|---|---|
| 加/改**学科** | `config/subjects/<学科>.json` | 复制现有文件改内容，文件名=学科名，立即可用 |
| 加/改**考点** | `config/reasons.json` | 追加一条（注意 JSON 逗号） |
| 改**输出格式** | `templates/output_format.md` | 用 `{占位符}` 排版，详见文件内 |
| 换**模型/调参** | `config/llm.json` | 改 `provider` 切换；`providers` 里加条目接新模型 |
| 改**出题总规则** | `templates/system_prompt.md` | 全局口吻、硬性要求 |

**学科文件字段**：`subject`(=文件名)、`domain_description`、`typical_scenarios`(情境清单,越具体题越好)、`common_symbols`、`typical_units`、`style_notes`。

**考点文件字段**（`reasons.json` 每条）：`label`、`math_structure`⭐、`numerical_method`、`must_have`⭐(硬约束,自检用)、`answer_pattern`、`pitfalls`。`math_structure` 和 `must_have` 写得越具体，题目越不跑偏。

**输出格式占位符**：`{subject}` `{reason_label}` `{check_status}` `{query}` `{approach}` `{code}` `{run_output}` `{answer}` `{answer_unit}` `{check_report}`。删占位符 = 该内容不出现；勿新增主代码不认识的占位符。

> 字段完整说明见 `config/README.md`。

---

## 7. Git 协同与版本更新

本项目已是 git 仓库并推送到远程，多人可协同维护。

### 7.1 日常自己改完存档

```bash
cd "/Users/v_wudongpei/Downloads/codeprompt Agent"
git add -A
git commit -m "改动说明，例如：新增光学学科"
git push
```

### 7.2 协作者首次加入

```bash
# 克隆仓库
git clone <仓库地址>
cd codeprompt-agent

# 装依赖 + 配自己的 Key
python3 -m pip install -r requirements.txt
export DEEPSEEK_API_KEY=sk-自己的key
```

> 每个人用**自己的** API Key（环境变量），Key 绝不进仓库。

### 7.3 多人协作标准流程（推荐用分支）

```bash
# 1. 开工前先拉最新代码
git pull

# 2. 建一个功能分支再改（避免直接动 main）
git checkout -b add-chemistry        # 例：新增化学学科

# 3. 改完提交
git add -A
git commit -m "新增化学学科与对应考点"

# 4. 推送分支
git push -u origin add-chemistry

# 5. 去 GitHub 网页发起 Pull Request，由他人 review 后合并进 main
```

### 7.4 拿别人的更新

```bash
git checkout main
git pull          # 同步远程最新
```

### 7.5 冲突 / 改错了怎么办

```bash
git status                  # 先看当前状态
git restore <文件>          # 放弃某文件的未提交改动
git pull 出现冲突           # 手动编辑冲突文件，再 git add + git commit
```

> 💡 协作约定建议：① 改配置（加学科/考点）走分支 + PR；② 提交信息写清楚改了什么；③ 不把 `output/`、Key 等提交（`.gitignore` 已处理）。

---

## 8. 常见问题排查

| 现象 | 解决 |
|---|---|
| `command not found: pip` | 改用 `python3 -m pip ...` |
| `环境变量 DEEPSEEK_API_KEY 未设置` | `export DEEPSEEK_API_KEY=...`，或先用 `--mock` |
| `未知的 subject/reason` | 用 `--list` 核对拼写，或去 `config/` 加对应文件/条目 |
| `代码运行报错 / 执行超时` | 会自动重试；可调大 `--timeout`、`--retries` |
| 题目跑偏 / 不契合考点 | `reasons.json` 把 `math_structure`、`must_have` 写更具体；或 `--judge block` |
| 网页 `cannot import name 'HfFolder'` | `python3 -m pip install "huggingface_hub<1.0"` |
| 网页 `bool is not iterable` / `localhost not accessible` | `app.py` 已内置补丁，直接重跑；仍不行则 `python3 -m pip install "gradio==4.31.5" "gradio_client==1.2.1" "huggingface_hub<1.0"` |
| 浏览器打不开 `127.0.0.1:7860` | 端口占用，`python3 app.py --port 8000` |
| `NotOpenSSLWarning` | 无害，忽略 |

---

## 9. 工作原理

```
输入：学科 + 不能手算的原因   （网页下拉框 / 命令行参数）
  │
  ├ [1] 读配置    config/subjects/*.json + config/reasons.json
  ├ [2] 组装提示  templates/system_prompt.md + generation_prompt.md
  ├ [3] 调模型    DeepSeek 生成「题面 + 思路 + 代码」
  ├ [4] 真实执行  ★子进程跑代码，抓 ANSWER 行★  ← 答案绝不靠模型臆造
  ├ [5] 自检      跑通？非线性到位？考点吻合？ 不通过→带反馈重试回[3]
  └ [6] 落盘      按 output_format.md 渲染 → output/*.md
```

网页版（`app.py`）与命令行版（`cli.py`）只是输入入口和展示方式不同，中间 [1]~[6] 是同一套引擎 `src/pipeline.py`。

> 🔒 安全：第 [4] 步会真实运行模型生成的代码，本机自用风险可控；若 `--share` 对外开放，正式部署前应给 `src/executor.py` 加更强沙箱隔离。

---

## 速查表

| 我想做什么 | 去哪里 |
|---|---|
| 开网页出题（推荐） | `python3 app.py` → 浏览器 `127.0.0.1:7860` |
| 命令行出题 | `python3 cli.py --subject X --reason Y` |
| 看可选学科/考点 | `python3 cli.py --list` |
| 看/下载结果 | `output/` 文件夹 |
| 加 / 改学科 | `config/subjects/*.json` |
| 加 / 改考点 | `config/reasons.json` |
| 改输出格式 | `templates/output_format.md` |
| 换模型 / 调参 | `config/llm.json` |
| 存档 / 推送 | `git add -A && git commit -m "..." && git push` |
| 协作改动 | 建分支 → 提交 → 推送 → PR（见 §7.3） |
