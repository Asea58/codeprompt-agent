请构造一道物理编程题。

## 本题约束（由系统给定）

- **学科背景**：{subject}
  - 领域说明：{domain_description}
  - 可取材的典型情境：{typical_scenarios}
  - 常用符号：{common_symbols}
  - 常用单位：{typical_units}
  - 风格要求：{style_notes}

- **核心考点（不能手算的原因）**：{reason_label}
  - 必须体现的数学结构：{math_structure}
  - 应采用的数值方法：{numerical_method}
  - **必须满足的硬约束**：
{must_have_block}
  - 答案形态：{answer_pattern}
  - 需规避的坑：{pitfalls}

{extra_requirements_block}

## 输出格式（严格遵守，便于程序解析）

必须依次输出以下用 XML 标签包裹的五个部分，标签独占一行：

<QUERY>
（完整题面：物理场景 + 全部参数数值 + 控制方程(LaTeX) + 初始/边界条件 + 明确的求解任务 + 保留小数位要求(1-2位)。
不得包含或依赖图片。题面**末尾必须原样附上**以下两行要求：
1. 使用Python语言（或其他语言）解答本题；
2. 完整写出代码的实现步骤。）
</QUERY>

<APPROACH>
（解题思路：为什么不能手算、采用什么数值方法、关键步骤。先讲原理再讲实现。用 Markdown。）
</APPROACH>

<CODE>
（完整可运行的 Python 代码，只用 numpy/scipy，运行时间控制在 5 秒内。
代码必须体现物理公式与数值算法，不可仅硬编码结果。
**求解必须真正收敛、答案必须物理合理：**
  - 求"事件/阈值时刻"用 `solve_ivp(events=...)`（`terminal=True`、正确 `direction`）精确捕捉，**禁止**用 `mask=数组<=阈值` 找首个满足点；积分区间要足够长，**事件若未在区间内发生必须 `raise`**，绝不能把积分区间端点当答案返回。
  - 边值/打靶问题优先用 `scipy.integrate.solve_bvp`；若用打靶法，须扫描定界 + `brentq` 求根并校验残差真正→0，**禁止**残差在极大量级振荡时仍当作收敛。
  - 迭代/求根**未收敛必须 `raise`**，不可返回末次迭代值充数。
  - 输出答案前按题面物理约束核对范围（如温度必在初温与环境温度之间）。
代码必须打印：
  - **4-6 行 `CHECK: <陈述句。>`**（不少于 4、不多于 6）：每行是一个"中间关键结果"，**必须严格满足以下全部格式要求**：
    1. 以 `CHECK: ` 开头（含冒号后一个空格）；
    2. 是一句**完整的中文自然语言陈述句**，以中文句号 `。` 结尾；
    3. **必须含该步由代码算出的一个数值**（带单位更好），如 `CHECK: 粒子在 t=2.34 s 时回旋半径达到最大值 0.87 m。`；
    4. **原子性**——一行只陈述一个中间量，不得用「且/，同时/以及」在一行里塞多个检查项；
    5. 只写**所有解法都绕不开的关键中间量**（拐点、极值、关键时刻/位置、稳态值等）。
  - **CHECK 行严禁出现以下内容**（出现即视为不合格）：
    - 复述题目给定的基础参数、初始条件或边界条件（那是已知量，不是"算出的中间结果"）；
    - 求解器实现细节：迭代次数、积分步数、网格点数、步长、容差、调用了什么函数等；
    - 不含数值的空泛描述（如"求解收敛了"、"结果合理"）。
  - 最后**单独打印一行** `ANSWER: <纯数值>`，不带单位。）
</CODE>

<ANSWER_UNIT>
（答案的单位与含义，一行，如：水平射程，单位 m）
</ANSWER_UNIT>

<I_CHECKLIST>
（检查清单分两部分，每条独占一行、以 `- ` 开头、以中文句号 `。` 结尾：
  - **答案部分（1 条）**：把问题所求结果写为**一条**，完整陈述句，含答案数值与物理含义（如 `- 本题答案：带电粒子的回旋半径为 0.87 m。`）。
  - **中间结果部分（4-6 条）**：写关键中间量，不少于 4 条、不多于 6 条，须与 CODE 中的 CHECK 行**一一对应**（同一批中间量、同样的数值）。
  - **每条的硬性格式**：完整中文陈述句、原子（一行只含一个检查项，不用「且/同时」堆叠）、含数值、客观、以 `。` 结尾。
  - **严禁**：复述题目给定的基础/初始/边界条件；写迭代次数、积分步数、步长、容差等实现细节；不含数值的空泛描述。
  - 数值部分可先按你的预期填写，程序会用代码真实运行值自动校正。）
</I_CHECKLIST>

不要输出任何标签以外的额外解释。
