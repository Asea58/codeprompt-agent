请构造一道物理编程题。

## 本题约束（由系统给定）

- **学科背景**：{subject}
  - 领域说明：{domain_description}
  - 可取材的典型情境：{typical_scenarios}
  - 常用符号：{common_symbols}
  - 常用单位：{typical_units}
  - 风格要求：{style_notes}

- **核心考点（不能手算的原因）**：{reason_label}
  - 必须体现的数学结构：{math_structure}
  - 应采用的数值方法：{numerical_method}
  - **必须满足的硬约束**：
{must_have_block}
  - 答案形态：{answer_pattern}
  - 需规避的坑：{pitfalls}

{extra_requirements_block}

## 输出格式（严格遵守，便于程序解析）

必须依次输出以下用 XML 标签包裹的六个部分，标签独占一行：

<QUERY>
（完整题面：物理场景 + 全部参数数值 + 控制方程(LaTeX) + 初始/边界条件 + 明确的求解任务 + 保留小数位要求(1-2位)。
不得包含或依赖图片。题面**末尾必须原样附上**以下两行要求：
1. 使用Python语言（或其他语言）解答本题；
2. 完整写出代码的实现步骤。）
</QUERY>

<APPROACH>
（解题思路：为什么不能手算、采用什么数值方法、关键步骤。先讲原理再讲实现。用 Markdown。）
</APPROACH>

<CODE>
（完整可运行的 Python 代码，只用 numpy/scipy，运行时间控制在 5 秒内。
代码必须体现物理公式与数值算法，不可仅硬编码结果。
代码必须打印：
  - **若干 `CHECK: <陈述句。>` 行**：每行是一个"中间关键结果"，用完整自然语言陈述句、含该步算出的数值、以句号结尾，
    例如 `CHECK: 系统在 t=2.34 s 时达到最大振幅 0.87 m。`。只写所有解法都绕不开的关键中间量（拐点、极值、关键时刻/位置等），3-4 条即可，**不要**复述题目给定的基础/边界条件。
  - 最后**单独打印一行** `ANSWER: <纯数值>`，不带单位。）
</CODE>

<ANSWER_UNIT>
（答案的单位与含义，一行，如：水平射程，单位 m）
</ANSWER_UNIT>

<I_CHECKLIST>
（4-6 条检查清单，每条一行、以 `- ` 开头、以句号结尾：
  - **第 1 条（必要时前 2 条）写最终答案**（用自然语言陈述，含答案数值与含义）。
  - 其后 3-4 条写**中间关键结果**，须与 CODE 中的 CHECK 行对应（同一批中间量）。
  - 每条必须是完整陈述句、原子（只含一个检查项）、客观、自然语言；删除一切基础/边界条件。
  - 数值部分可先按你的预期填写，程序会用代码真实运行值校正。）
</I_CHECKLIST>

<CHECKLIST_NEW>
（分两部分，每条一行、以 `- ` 开头：
  - **第一部分（必须有）**：问题的答案。
  - **第二部分（视情况）**：仅当题目明确要求使用指定算法（如"使用蒙特卡洛方法"）时，才写出该指定解题方法；否则省略此部分。）
</CHECKLIST_NEW>

不要输出任何标签以外的额外解释。
